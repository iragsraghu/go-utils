# Go Utils

A collection of reusable utility packages written in Go (Golang) that can be used across multiple projects.

## Installation

Install the package using:

```bash
go get github.com/iragsraghu/go-utils